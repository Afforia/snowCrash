ls -la
видим экзекютабл файл level03, который при запуске выводит строку "Exploit me"
и запускается от пользователя(user) файла

смотри ltraceом что происходит внутри файла.
видим что в систем вызывется echo
создаем мягкую ссылку на getflag и называем ее echo

с помощью временной переменной PATH (путь к нашему поддельному echo) и запускаем level03

profit
